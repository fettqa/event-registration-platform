name: Mobile Appium Python

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'android/**'
      - 'tests-mobile/python/**'
      - 'tests-mobile/scripts/**'
      - '.github/workflows/mobile-appium-python.yml'
  pull_request:
    branches: [main]
    paths:
      - 'app/**'
      - 'android/**'
      - 'tests-mobile/python/**'
      - 'tests-mobile/scripts/**'
      - '.github/workflows/mobile-appium-python.yml'

jobs:
  appium-python:
    name: Appium Python
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: tests-mobile/python/requirements.txt

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Make gradlew executable
        run: |
          chmod +x app/gradlew
          chmod +x android/gradlew
          chmod +x tests-mobile/scripts/ci-run-suite.sh

      - name: Build Spring Boot jar
        working-directory: app
        run: ./gradlew bootJar -x test

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Start Spring Boot
        working-directory: app
        run: |
          JAR=$(ls build/libs/*SNAPSHOT.jar | head -n 1)
          echo "Starting $JAR"
          nohup java -jar "$JAR" > app.log 2>&1 &
          echo $! > app.pid

      - name: Wait for app
        working-directory: app
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8080/actuator/health | grep -q UP; then
              echo "App is UP"
              exit 0
            fi
            echo "Waiting... ($i)"
            sleep 2
          done
          echo "App failed to start"
          cat app.log || true
          exit 1

      - name: Install Python test deps
        working-directory: tests-mobile/python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Appium + UiAutomator2
        run: |
          npm install -g appium
          appium driver install uiautomator2

      - name: Start Appium
        run: |
          appium --address 127.0.0.1 --port 4723 > appium.log 2>&1 &
          echo $! > appium.pid
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:4723/status | grep -q ready; then
              echo "Appium is ready"
              exit 0
            fi
            echo "Waiting for Appium... ($i)"
            sleep 2
          done
          echo "Appium failed to start"
          cat appium.log || true
          exit 1

      - name: Run Appium Python on emulator
        uses: reactivecircus/android-emulator-runner@v2
        env:
          ANDROID_APK: ${{ github.workspace }}/android/app/build/outputs/apk/debug/app-debug.apk
          APPIUM_URL: http://127.0.0.1:4723
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          script: ./tests-mobile/scripts/ci-run-suite.sh python

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-mobile-python
          path: tests-mobile/python/allure-results
          if-no-files-found: ignore

      - name: Generate Allure HTML report
        if: always()
        working-directory: tests-mobile/python
        run: |
          if [ -d allure-results ] && [ "$(ls -A allure-results 2>/dev/null)" ]; then
            npm install -g allure-commandline@2.29.0
            allure generate allure-results -o allure-report --clean
          fi

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-mobile-python
          path: tests-mobile/python/allure-report
          if-no-files-found: ignore

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-appium-python-logs
          path: |
            app/app.log
            appium.log
          if-no-files-found: ignore

      - name: Stop Appium
        if: always()
        run: |
          if [ -f appium.pid ]; then kill "$(cat appium.pid)" || true; fi

      - name: Stop Spring Boot
        if: always()
        working-directory: app
        run: |
          if [ -f app.pid ]; then kill "$(cat app.pid)" || true; fi

  publish-allure-report:
    name: Publish Allure Report
    runs-on: ubuntu-latest
    needs: appium-python
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    permissions:
      contents: write
    steps:
      - name: Download Allure Report
        uses: actions/download-artifact@v4
        with:
          name: allure-report-mobile-python
          path: allure-report

      - name: Publish to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report
          destination_dir: allure/mobile-python/${{ github.run_number }}
          keep_files: true

      - name: Summary
        run: |
          REPORT_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/allure/mobile-python/${{ github.run_number }}"
          echo "## Allure report" >> "$GITHUB_STEP_SUMMARY"
          echo "- [View Allure Report](${REPORT_URL})" >> "$GITHUB_STEP_SUMMARY"
