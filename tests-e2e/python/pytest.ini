[pytest]
testpaths = functional
python_files = test_*.py
pythonpath = .
addopts = -v --browser chromium --alluredir=allure-results
markers =
    smoke: critical-path smoke
    mobile_chrome: web UI on Mobile Chrome (Playwright Pixel 7 emulation)
