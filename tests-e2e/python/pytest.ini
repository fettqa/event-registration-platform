[pytest]
testpaths = functional
python_files = test_*.py
pythonpath = .
addopts = -v --browser chromium --alluredir=allure-results
markers =
    mail: needs Mailpit + app profiles kafka,mail
    bdd: Gherkin scenarios (pytest-bdd)
